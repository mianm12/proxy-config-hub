# 当前行为测试输入

这里的输入只使用虚构端点和脱敏名称，用于验证当前配置编译、override 与 rename 行为，不是可用订阅。

- `override/`：覆盖节点分类、地区识别、链路退化、宿主字段和完整配置装配。
- `rename/`：使用命名 profile 验证 Sub-Store 节点重命名。
- `tests/expected/`：保存经过人工审阅的结构化输出；行为变更必须同步修改对应测试和预期结果。

边界和错误场景优先使用针对不变量的断言，避免为不需要完整结构对比的场景保存大型快照。
